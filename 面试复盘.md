# 自我介绍、介绍自己的项目、项目中遇到的难点

# 前端本地存储？

- 为什么选择了 sessionStorage 而没有采用其他的本地存储方式？
- localStorage 除了存储大小上的区别还有哪些区别？

# 请求服务使用的是 axios,有对它做过什么封装吗？

- 它的底层实现
- 浏览器发起请求的两种原生方式：XMLHttprequest fetch 区别

# vue 的双向绑定

# 有了解过 vue3 吗？和 vue2 有什么区别？

- vue3 和 vue2 最大的区别就是监听方式的变更
- vue3 用的是 proxy,vue2 用的是 defineProperty

# const a={}, 调用 a.toString() 会报错吗？输出什么？

- 怎么判断 a 是一个空对象

# for...in for...of 分别应用的场景是什么？

- for..of 遍历什么比较比较多呢？
- ES6 新增的 Set Map

# 跨域？跨域的方式？

# 实现两列布局，左边宽度固定，右边宽度自适应

# 平时是怎么去学习前端的？

# 反问环节

# DOM

将下面所有 li 标签中 id 的值赋给 li 标签内部的 input 输入框中。
\<ul>
\<li id="li_1">\<input type="text">\</li>
\<li id="li_2">\<input type="text">\</li>
\<li id="li_3">\<input type="text">\</li>
\<li id="li_4">\<input type="text">\</li>
\<li id="li_5">\<input type="text">\</li>
\</ul>

```js
document.querySelectorAll("li").forEach((child) => {
  let id = child.getAttribute("id");
  child.childNodes[0].value = id;
});
```

### 贝壳面试复盘

#### 一面

##### 1.合并两个有序数组

```js
var merge = function (nums1, m, nums2, n) {
  let i = m - 1;
  let j = n - 1;
  let k = m + n - 1;
  while (i >= 0 && j >= 0) {
    if (nums1[i] >= nums2[j]) {
      nums1[k] = nums1[i];
      i--;
      k--;
    } else {
      nums1[k] = nums2[j];
      j--;
      k--;
    }
  }
  while (j >= 0) {
    nums1[k] = nums2[j];
    j--;
    k--;
  }
};
```

##### 2.手写 bind

```js
Function.prototype.mybind = function (obj, ...args1) {
  if (typeof this !== "function") throw new Error("not function");
  let self = this; //接收外部是谁调用bind
  let Fmiddle = function () {};
  let Fn = function (...args2) {
    return self.apply(
      this instanceof Fmiddle ? this : obj,
      [...args1].contact([...args2])
    ); //判断是new绑定还是直接绑定，new绑定就绑定new调用的this，直接绑定的话 this指向上下文
  };
  //如果调用bind的方法是原型上的方法，需要将返回函数的原型链指向调用bind的方法 使得A的实例可以使用Animal的属性
  Fmiddle.prototype = this.prototype; //不使用Fn.prototype = this.prototype 原因是Fn.prototype改变时 this.prototype也会改变 所以用一个空函数作为桥梁 执行完空函数就销毁了
  Fn.prototype = new Fmiddle();
  return Fn;
};
```

##### 3.项目后端跨域 cors

##### 4.数组哪些操作方法

##### 5.有哪些方法可以取 dom?有什么不同（getElementbyXXX、querySelectXXX）

##### 6.谈谈 HTTP（思路：http 发展史啥的，http1.0/1.1/2.0/3 对比优缺点，基于 TCP）

#### 二面

##### 1.设计一个输入支付密码/登录密码组件（输入输出，双绑，传输安全，可复用）

##### 2.假设一个业务有三个流程分别是 ABC，不同城市有不同的流程顺序（比如说北京是 ABC 上海是 ACB 杭州是 BAC 等等），如何设计（从前端角度、后端角度，ssr）

##### 3.上面的业务预计三天完成，主管要求一天完成，并加派了两个人，如何调度？

# 字节

# nextTick 的原理实现

# vue-router 的原理实现

# 浏览器的事件模型实现过程

- 比如点击按钮，它具体是怎么实现操作的

# Event Loop 整体的机制

- 宏任务、微任务有哪些

# 箭头函数的 this 和普通函数的 this

- 有什么区别
- 分别指向什么

# 看代码写输出

- var （有包括箭头函数的指向）
- 将 var 改为 let 的输出
- Event Loop 中的输出

# position 的属性有哪些？

- absolute 是怎么实现的
- 如果父级元素没有 position 时怎么往上寻找
- position:static

# 手写居中的正方形

# 手写 Promise.all()

# header、footer 定高，中间自适应

# 基本数据类型为什么存在栈内存里，引用数据类型为什么存在堆内存里

# token 和 seesionID（key）有什么不同

# 有什么请求是天然可以跨域的

# 长度 2N N+1 个不同数 有一个数重复出现 N 次，找出这个数

# symbol

# express 中间件函数实现

# stoppropogation 和 stopimmediatepropogation 区别

# jd

# url 输入到页面渲染发生了什么

# 六种继承

# es6 新特性

# 浏览器缓存

# 跨域
- https://www.ruanyifeng.com/blog/2016/04/cors.html
